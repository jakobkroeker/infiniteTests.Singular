version="ring";
category="Commutative Algebra";
info="";

proc getRing()
{
  //////// adjust ring  
  string cchar    = "0";
  string ordering = "(lp(2),lp(3))";
  int    numVars  = 3;

  ring rng = 0,(r,s,x,y,z),(lp(2),lp(1));

  option("redSB");
  option("redTail");
  return (rng);
}


// char_0.vars_3.ordering_lp(3)_lp(3).redSB.redTail