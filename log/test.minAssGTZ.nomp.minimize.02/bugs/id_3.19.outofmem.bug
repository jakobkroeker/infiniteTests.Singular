

////////////////////////////////////////////////////////////////////////////////////////
if (not defined(assumeLevel)) { def Top::assumeLevel ; } assumeLevel = 3;  
//  voice = 3;
system("random",761992850);
//random: 761992850
LIB("src/tests/test.primdec.mp.timeout");
intvec op = 67108864,16787537;
ring rng = (0),(x,y,z),(lp(1),dp(2),C);
minpoly = 0;
ideal I = 2*x^2*z-16*x*y^3-8,-7*x^4+8*x*z^2-2*y^2*z^2;
printIdealInfo( I ) ;
// numVars     = 3;
// maxAbsCoeff = 16;
// maxDeg      = 4;
// maxTerms    = 3;
// numGens     = 2;
bigint trial = 0;
def result= minAssGTZ(I);
quit;
quit;
