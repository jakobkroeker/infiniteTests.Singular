

////////////////////////////////////////////////////////////////////////////////////////
if (not defined(assumeLevel)) { def Top::assumeLevel ; } assumeLevel = 3;  
//  voice = 3;
system("random",72952576);
//random: 72952576
LIB("bugs/tests/test.primdec.mp.timeout");
intvec op = 67108864,16787537;
ring rng = (0),(x,y,z,e),(lp(1),lp(1),Dp(2),C);
minpoly = 0;
ideal I = -8*x*e+12*z^2+3*z,-22*y^3+16*y*e^2+28*z*e,7*x^2*e-9*y^2*e-17*y;
printIdealInfo( I ) ;
// numVars     = 4;
// maxAbsCoeff = 28;
// maxDeg      = 3;
// maxTerms    = 3;
// numGens     = 3;
bigint trial = 0;
def result= minAssGTZ(I);
quit;
quit;
