

////////////////////////////////////////////////////////////////////////////////////////
if (not defined(assumeLevel)) { def Top::assumeLevel ; } assumeLevel = 3;  
//  voice = 3;
system("random",1296990471);
//random: 1296990471
LIB("bugs/test.primdec.mp.timeout");
intvec op = 67108864,16787537;
ring rng = (0),(x,y,z,vv),(Dp(2),lp(1),lp(1),C);
minpoly = 0;
ideal I = 208*x*z+272*z*vv-226*vv;
bigint trial = 0;
def result= primdecGTZ(I);
quit;
