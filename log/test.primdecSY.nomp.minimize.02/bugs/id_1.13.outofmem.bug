

////////////////////////////////////////////////////////////////////////////////////////
if (not defined(assumeLevel)) { def Top::assumeLevel ; } assumeLevel = 3;  
//  voice = 3;
system("random",1815915699);
//random: 1815915699
LIB("src/tests/test.primdec.mp.timeout");
intvec op = 0,16787537;
ring rng = (107),(x,y,z,vv),(lp(1),lp(1),lp(2),C);
minpoly = 0;
ideal I = 26*x^2*vv^2+23*y^2*z*vv-21,23*x*y^2-5*y*z-9*vv;
printIdealInfo( I ) ;
// numVars     = 4;
// maxAbsCoeff = -5;
// maxDeg      = 4;
// maxTerms    = 3;
// numGens     = 2;
bigint trial = 0;
def result= primdecSY(I);
quit;
quit;
