system("random", 523454);
LIB("primdec.lib");
LIB("ehv.lib");
ring rng = 23,(x, y, z), dp;
ideal I = 9*x*y^2-11*x*z^2, 11*x*y*z^2+4*x^2*z, -3*y^5+x*z-10*y*z;

list L1,L2;

def RI = std(radical(I));

TRACE=1;
echo = 6;
L1 = primdecGTZ(RI);
L1;
