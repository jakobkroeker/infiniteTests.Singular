

LIB("bugs/test.std.correct");
//monitor("log/test.std.correct.07","io");



//////////////////////////////////////////////////////////////////////////////// 
//////// adjust verbosity
printlevel = 0 ;
TRACE = 0;
echo  = 0;

//////// adjust random ideal generator params: 
def genParams = defaultRandomConstructionParams();

genParams.absMaxCoeff       = 50;
genParams.bFractionsOn = 0;
genParams.minVarFactorsPerMonomial = 0;
genParams.maxVarFactorsPerMonomial = 2;
genParams.maxTermsPerGen    = 2;
genParams.maxGens           = 3;

def gens = createRandomGeneratorsByParams( genParams  );


//def rng = createRing( cchar, ordering, numVars ) ;
ring rng = integer,(x,y,z,m,l),(lp(2),lp(3));

option("nowarn");
option(redSB);
option(redTail);

//////// adjust num of trials  
int trials = 100000000;
//////////////////////////////////////////////////////////////////////////////// 
 

testGroebner( rng, gens.idealG, trials, logfile);

// ulimit -v 1000000 ; ulimit -t 500
//  set -o pipefail; ./singular-spielwiese < input/test.std.correct.07 2>&1 | tee -a log/test.std.correct.07.log ;
// export cont=1; while [ $cont -eq 1 ] ; do set -o pipefail; nice -19 ./singular-spielwiese < input/test.std.correct.07 2>&1 | tee -a log/test.std.correct.07.log ; if [ $? -eq 123 ] ; then export cont=0; fi; done
